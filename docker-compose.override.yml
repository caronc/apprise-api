services:
  apprise:
    build:
      context: .
      dockerfile: Dockerfile
    image: apprise-api-dev
    container_name: apprise-api-dev
    user: "${PUID:-1000}:${PGID:-1000}"
    environment:
      APPRISE_STATEFUL_MODE: simple
      APPRISE_WORKER_COUNT: "1"
    volumes:
      - ./apprise_api:/opt/apprise/webapp:ro
      - ./config:/config
      - ./plugin:/plugin
      - ./attach:/attach
